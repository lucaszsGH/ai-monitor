# Versioning

This project follows semantic versioning while in release-candidate development.

- Update `VERSION`.
- Update the English and Chinese README status lines.
- Add a dated CHANGELOG entry.
- Re-run package validation, tests, generated-starter validation, and Quality Gate.
- Never reuse a release tag for different content.

