# Intro assets

Editable SVG and rendered PNG hero/workflow pairs are provided in English and Simplified Chinese.

The product name and Home Screen name are both **AI Monitor**. **DeepWheel** remains the brand and official app-icon identity. Before the first tagged release, the technical Skill identifier was corrected to `lucas-deepwheel-ai-monitor` so the public package uses one consistent name.

The workflow pair mirrors the first-run guide: generate a synthetic demo, validate it locally, preview it on the desktop, confirm trusted-LAN sharing, check the iPhone landscape experience, and only then choose whether to connect a read-only live-data source. LAN exposure and live-data access remain separate approval steps.

The hero pair and phone renders use the current multi-session information architecture and the Lucas-approved v4 visual baseline: adaptive landscape type, semantic quota colours, shared session alignment, the two-row NOW optical correction and the accepted usage hierarchy. Obsolete single-session concept art and the superseded smaller-type public render are intentionally excluded.

`ai-monitor-phone-landscape-apple-17promax-final.png` is the Chinese AI Monitor view captured from the executable equal-split HTML/CSS implementation at 956×440 logical points and stored at 1912×880 pixels for documentation. It injects a conservative 62px horizontal obstruction guard for rendering QA.

`ai-monitor-phone-landscape-apple-iphonex-final.png` is the English AI Monitor view of the same implementation captured at 812×375 logical points and stored at 1624×750 pixels. It injects a conservative 44px horizontal obstruction guard for rendering QA.

Earlier failed and superseded local render iterations were removed after owner approval and must not be recreated in a public commit or release.
